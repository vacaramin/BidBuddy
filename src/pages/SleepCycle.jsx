import styled from "styled-components";

const SleepCycle = ({ className }) => {
  return (
    <div className={className}>
      <h1>Sleep Cycle</h1>
    </div>
  );
};

export default styled(SleepCycle)`

`;
